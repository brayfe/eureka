id: content

# A human-friendly label for the group.
label: Content Types

# More information about the group.
description: The content associated with the new Eureka site.

source_type: Custom tables

shared_configuration:
  source:
    key: legacy

dependencies:
  enforced:
    module:
      - eureka_migration
